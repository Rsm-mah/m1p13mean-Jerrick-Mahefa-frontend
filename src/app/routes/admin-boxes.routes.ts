import { Routes } from '@angular/router';
import { BoxesComponent } from '../components/boxes/boxes';

export const AdminBoxesRoutes: Routes = [
  { path: 'admin/boxes', component: BoxesComponent }
];

export default AdminBoxesRoutes;
